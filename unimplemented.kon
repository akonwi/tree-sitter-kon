// cursor tuple including index
for item, _ in ["a", "b", "c"] {
	print(i)
}

// iterate on maps
let map = ["a": 1, "b": 2, "c": 3]
for key, value in map {
	print(i)
}

// structs as enum variants
enum Payload {
  Plain,
  Object { id: Str }
}

guard/unless operator
